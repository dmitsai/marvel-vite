import React from "react";
import './styles.css';

export const SearchButton: React.FC = () => {
    return (
        <button className="btn" type='submit'>
            {'SEARCH'}
        </button>

    )
}